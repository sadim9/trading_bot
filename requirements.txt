# ── Core Data & Math ──────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0

# ── Market Data ───────────────────────────────────────────────────
yfinance>=0.2.36

# ── Machine Learning ──────────────────────────────────────────────
scikit-learn>=1.3.0

# ── Dashboard ─────────────────────────────────────────────────────
streamlit>=1.35.0

# ── Plotting (Streamlit charts + trade journal) ───────────────────
plotly>=5.18.0

# ── HTTP (alerts, BitOasis broker, API client) ────────────────────
requests>=2.31.0

# ── Discord (trade confirmation bot with interactive buttons) ─────
# Pin aiohttp<3.12 — aiohttp 3.13.x wheel causes SSL_UNEXPECTED_EOF
# on DigitalOcean during docker builds (large wheel + TLS fragmentation)
aiohttp>=3.9.0,<3.12.0
discord.py>=2.3.0

# ── Dev / Testing ─────────────────────────────────────────────────
# pytest>=7.4.0

# ── TradingView data (unofficial free API) ────────────────────────────────────
# tvdatafeed-enhanced: PyPI-published successor to the defunct StreamAlpha/tvdatafeed.
# Maintained by rongardF/StreamAlpha, supports Python 3.11+, no git clone needed.
tvdatafeed-enhanced>=2.2.1
websocket-client>=1.7.0   # required by tvdatafeed-enhanced (imports websocket.create_connection)
# tradingview-ta: Real-time technical analysis summaries from TradingView
tradingview-ta>=3.3.0
